trigger:
- main

jobs:
 - job: UnitTest
   pool:
     vmImage: 'macOS-latest'

   steps:
    - task: NodeTool@0
      inputs:
       versionSpec: '16.x'

    - script: |
        npm ci
        npm test
      displayName: 'npm test'
